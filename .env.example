# Ports (change if you get "port already allocated")
NGINX_HTTP_PORT=20200
MARIADB_PORT=20201
PHPMYADMIN_PORT=20202

# App
WP_URL=http://localhost:20200
WP_TITLE=Framework Dev
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=demo
WP_ADMIN_EMAIL=admin@example.com

# Database
DB_NAME=framework_dev
DB_USER=wordpress
DB_PASSWORD=wordpress
DB_ROOT_PASSWORD=root

# Xdebug (trigger mode: debugging starts only with XDEBUG_TRIGGER or a browser extension)
XDEBUG_MODE=off
XDEBUG_CLIENT_HOST=host.docker.internal
XDEBUG_CLIENT_PORT=9003
XDEBUG_IDEKEY=PHPSTORM
